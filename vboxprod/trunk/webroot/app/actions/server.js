/**
 * Server actions
 */
Ext.define('vcube.actions.server',{

	statics: {
		
		'new': {
			
			action: function(selectionModel) {
				
			}
		},
		
		settings: {
			action: function(selectionModel) {
				
			}
		},
		
		/** Delete / Remove a server */
		'remove': {
			action: function(selectionModel) {
				
			}
		}
	}
	
});
